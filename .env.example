# Okta Integrator (free) tenant credentials.
# UNUSED in Milestone 1 (static, file-only). Needed starting M2 (live read).
#
# Placeholder values ONLY. Real secrets go in `.env` (gitignored), never here.
# All API work uses READ-ONLY scopes: okta.groups.read, okta.apps.read, okta.policies.read.

OKTA_ORG_URL=https://your-org.okta.com
OKTA_API_TOKEN=your-read-only-api-token
